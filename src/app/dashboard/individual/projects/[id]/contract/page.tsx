"use client";

import { ContractPage } from "@/features/contract";



export default function IndividualContractPage() {
  // In a real app, you would fetch the contract based on project ID
  // For now, we're using the mock data from the store
  
  return <ContractPage />;
}
